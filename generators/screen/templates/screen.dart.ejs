<%- importPackages %>

class <%= screenNameUpperCase %>__Screen extends FGS_UTILS__BaseWidget {
  const <%= screenNameUpperCase %>__Screen({ 
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
